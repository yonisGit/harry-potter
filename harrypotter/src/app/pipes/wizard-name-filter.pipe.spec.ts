import { WizardNameFilterPipe } from './wizard-name-filter.pipe';

describe('WizardNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WizardNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
